spring:
  application:
    name: cache-write
  redis:
    database: 0
    host:
    port:
    password:
    pool:
      max-active: 8
      max-wait: -1
      max-idle: 8
      min-idle: 0
    timeout: 0
