<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.ffcs.uoo.organization.in.action.db.OrganizationMapper">

    <sql id="organization_all_column">
        id, gmt_create, gmt_modified, name
    </sql>

    <resultMap id="organization_all_column_result_map" type="cn.ffcs.uoo.organization.in.action.domain.Organization">
        <id property="id" column="id"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
        <result property="name" column="name"/>
    </resultMap>

    <select id="list" resultMap="organization_all_column_result_map">
        SELECT <include refid="organization_all_column"/> FROM organization
    </select>

    <select id="get" resultMap="organization_all_column_result_map">
        SELECT <include refid="organization_all_column"/> FROM organization WHERE id = #{id}
    </select>

    <insert id="save">
        INSERT INTO organization (<include refid="organization_all_column"/>) VALUES (null, now(), now(), #{name})
    </insert>

    <delete id="remove">
        DELETE FROM organization
        <where>
            <if test="id != null and id != ''">
                id = #{id}
            </if>
            <if test="name != null and name !=''">
                AND name = #{name}
            </if>
        </where>
    </delete>

    <update id="update">
        UPDATE organization
        <set>
            gmt_modified = now()
            <if test="name != null and name != ''">
                , name = #{name}
            </if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>
